#include "common.h"

//////////////////////////////////////////////////////////////////////////////////////////
// Pixel
float4 main ( p_TL I ) : SV_Target
{
	float4 tex = s_base.Sample( smp_base, I.Tex0 )*I.Color;

	return display_to_intermediate_space(tex);

//	float4 res = s_base.Sample( smp_base, I.Tex0 )*I.Color;
//	clip(res-m_AlphaRef);
//	clip(res-0.5);
//	return res;
}
