// [ SETTINGS ] [ FOG ]

#define G_FOG_HEIGHT					fog_ground_params.x	// Height of the ground fog.
#define G_FOG_HEIGHT_INTENSITY			fog_ground_params.y	// Intensity of the ground fog.
#define G_FOG_HEIGHT_DENSITY			fog_ground_params.z	// Density of the ground fog.

#define G_FOG_SUNCOLOR_INTENSITY		fog_ground_params.w	// Intensity of sun color in the fog. [ 0.0f = 0% | 1.0f = 100% ]